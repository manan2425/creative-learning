# Image Upgrade Folder

- `source-pages/` = rendered catalogue pages kept as source/reference images.
- `components/` = cropped product/component images used by the storefront.
- `branding/` = header/logo source crop.

To upgrade the website, drop new product images into `components/` and point the matching `image` field in `data/products.json` to the new file.
